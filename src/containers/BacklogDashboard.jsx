import React from 'react';

import { observer } from 'mobx-react';

const BacklogDashboard = observer(({ store }) => {
  return (
    <div className="backlogDashboard">
    </div>
  )
});

export default BacklogDashboard;